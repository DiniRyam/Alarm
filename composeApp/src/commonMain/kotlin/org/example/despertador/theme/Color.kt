package org.example.despertador.theme

import androidx.compose.ui.graphics.Color

// cores base para o card
val GlassWhite = Color(0xFFFFFFFF).copy(alpha = 0.15f)
val GlassBorder = Color(0xFFFFFFFF).copy(alpha = 0.3f)

// cores extras para os buttons
val SelectionPurple = Color(0xFFBB86FC).copy(alpha = 0.5f)
val VibrationYellow = Color(0xFFFFF176).copy(alpha = 0.6f)
val FabRed = Color(0xFFFF5252).copy(alpha = 0.7f)
val SwitchGreen = Color(0xFF4ADE80)